# dns-models

Shared DNS models using Beanie and Pydantic. Install in other services with a path or VCS dependency, for example:

pip install -e ../libs/models

Or add to your environment's dependencies as appropriate.
